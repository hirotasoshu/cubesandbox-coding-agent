# Test Workspace

This small Python project is uploaded to `/workspace` by the Codex and OpenCode
E2B examples. Each example downloads the resulting workspace as a `.tar.gz`
archive after the agent finishes.
